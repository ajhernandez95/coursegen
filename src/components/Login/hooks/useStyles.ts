const useStyles = () => {
  const boxStyles = {
    display: "flex",
    margin: "auto",
    paddingTop: "150px",
    justifyContent: "center",
  };

  const cardStyles = {
    padding: "10px 30px",
  };

  const textStyles = {
    fontSize: "2xl",
  };

  const formControlStyles = {
    mt: "15px",
  };

  const buttonStyles = {
    mt: "15px",
  };

  return {
    boxStyles,
    cardStyles,
    textStyles,
    formControlStyles,
    buttonStyles,
  };
};

export default useStyles;
