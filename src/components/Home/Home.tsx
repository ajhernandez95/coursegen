import { Text } from "@chakra-ui/layout";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <div>
      <Text fontSize="1xl">Home</Text>
    </div>
  );
};

export default Home;
