import React from "react";

const useStyles = () => {
  const boxContainer = {
    padding: "0 10rem",
    margin: "auto",
  };

  const formContainer = {
    width: "100%",
  };
  return {
    boxContainer,
    formContainer,
  };
};

export default useStyles;
