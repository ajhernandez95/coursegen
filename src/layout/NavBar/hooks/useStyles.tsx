const useStyles = () => {
  const navbarStyles = {
    // display: "flex",
    // alignItems: "center",
    // justifyContent: "space-between",
    // padding: "0 50px",
  };

  const textStyles = {
    fontSize: "3xl",
    fontWeight: "bold",
  };

  return { navbarStyles, textStyles };
};

export default useStyles;
