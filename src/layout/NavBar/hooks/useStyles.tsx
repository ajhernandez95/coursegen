const useStyles = () => {
  const navbarStyles = {
    h: "60px",
    display: "flex",
    justifyContent: "space-between",
    padding: "5px 20px",
  };

  const textStyles = {
    fontSize: "3xl",
  };

  return { navbarStyles, textStyles };
};

export default useStyles;
